import React, { useEffect, useContext } from 'react';
import { useParams } from 'react-router-dom';
import context from '../contexts/context';

function AboutServants() {
  // const contexts = useContext(context);
  // const { data } = contexts;
  // const params = useParams();

  //   const fetchId = () => {
  //       const filteredData = data.filter((item) => {
  //         return Object.values(item)
  //           .join('')
  //           .toLowerCase()
  //           .includes(params.toLowerCase());
  //       });
  //      // setIdInfo(filteredData);
  //       console.log(data);
  //   };

  //   fetchId();


  return (
    <div>
      oi

      {/* nome, classe, imagem nos stages 1, 2, 3 e 4, ataques, HP, voice actor, illustrator, attribute, growth curve, alignments, gender, traits, type cards, etc. */}
    </div>
  );
}

export default AboutServants;
